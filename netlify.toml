[build]
  publish = "."

# No build command — this is a plain static HTML site.
# Netlify serves files directly from the repo root.

[[redirects]]
  from = "/store"
  to   = "/store.html"
  status = 200
